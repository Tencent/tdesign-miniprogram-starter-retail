<view class="tip-wrap">
	<view class="img">
		<image class="cart-img" src="{{imgUrl}}" />
	</view>
	<view class="tip">{{tip}}</view>
	<view class="btn" bindtap="handleClick">{{btnText}}</view>
</view>

